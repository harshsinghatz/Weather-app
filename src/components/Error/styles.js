import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  error: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    gap: "2.4rem",
    color: "#ff6e40",
    margin: "10rem 0",
  },
  icon: {
    fontSize: "4rem",
  },
}));
